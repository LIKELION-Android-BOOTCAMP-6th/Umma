# Demo Scenario - FLOW-WATCH-NOTIFICATION

이 문서는 Umma Wear OS Companion V1의 `워치 알림 미러링 데모/QA 기준`이다.

V1 알림 정책은 다음을 전제로 한다.

- 알림의 source of truth는 `폰`이다.
- 알림 생성은 기존 폰 FCM/로컬 알림 경로를 사용한다.
- 워치는 시스템 notification mirroring으로 알림을 받는다.
- V1에서 워치 자체 FCM token 등록 및 direct watch push는 구현하지 않는다.
- 현재 워치 알림에서 핵심 검증 대상은 `SRS 알림 미러링`이다.
- 워치는 알림 SSOT가 아니라 `폰 알림의 mirrored surface`로 동작한다.
- 워치 알림 미러링은 `폰 전원 ON + 워치 페어링 유지 + 워치 연결 가능 상태`를 전제로 한다.
- 워치 연결 경로는 `Bluetooth` 또는 `Wi-Fi`이며, LTE 지원 모델은 `LTE`도 가능하다.
- 같은 Wi-Fi SSID 연결은 필수 전제가 아니다.
- 워치 액션과 폰 handoff는 별도 구현 범위이므로, 본 문서는 미러링 수신과 노출 정책 기준을 우선 다룬다.

관련 문서:

- `docs/System_FlowDB/SYS_WATCH_INFRA.md`
- `docs/Sprint2/User_FlowDB/FLOW_SRS.md`
- `docs/drawio/06_srs_review_pipeline.drawio`
- `app/src/main/java/com/app/umma/data/push/UmmaFirebaseMessagingService.kt`
- `docs/Sprint3/demo/DEMO_FLOW_PHONE_NOTIFICATION.md`

---

## 1. 실행 기준

- 폰 앱은 로그인된 상태여야 한다.
- 폰의 알림 권한은 허용 상태여야 한다.
- 워치는 폰과 페어링되어 있어야 한다.
- 폰은 켜져 있어야 한다.
- 워치는 Bluetooth 또는 Wi-Fi로 연결 가능해야 하며, LTE 지원 모델은 LTE 연결도 가능하다.
- V1 기준 워치 알림은 `폰에서 생성된 알림이 워치로 미러링되는지`를 검증한다.

데모에서 확인할 핵심은 아래 세 가지다.

- 워치에서 SRS 알림이 정상적으로 보이는지
- direct watch push 없이도 폰 알림이 워치로 전달되는지
- 워치 알림 본문이 짧고 안전하게 노출되는지

---

## 2. 현재 데모 범위

- 포함:
  - SRS 알림 미러링
  - 워치 알림 제목/본문 노출 검증
  - 민감 정보 미노출 검증
- 제외:
  - 워치 알림 액션
  - 폰 handoff
  - snooze 재스케줄
  - watch direct push
  - watch token registration
  - Tile/Complication
  - 마케팅 알림 전용 워치 UX

---

## 3. 데모 시나리오

### 시나리오 1 - SRS 알림 미러링 수신

1. 폰에서 SRS 알림이 생성되도록 due flashcard 상태를 준비한다.
2. 폰에 SRS 알림이 표시되는지 확인한다.
3. 같은 알림이 워치에 미러링되어 표시되는지 확인한다.
4. 워치 알림 제목/본문이 과도하게 길지 않고 한눈에 읽히는지 확인한다.

기대 결과:

- 워치가 direct push 없이도 폰 알림을 동일한 logical event로 보여준다.
- 워치 알림 본문은 짧고 절삭된 형태로 노출된다.
- 민감한 상세 데이터는 노출되지 않는다.

---

### 시나리오 2 - due card 변경 후 알림 확인

1. 폰에서 due flashcard 상태를 변경한다.
2. 새 SRS 알림이 생성되도록 유도한다.
3. 워치에서 새 알림이 정상적으로 갱신되어 보이는지 확인한다.

기대 결과:

- 워치는 폰 알림의 최신 상태를 미러링한다.
- 이미 소진된 과거 상태가 잘못 유지되지 않는다.

---

### 시나리오 3 - 알림 내용 길이 / 민감도 확인

1. 긴 제목 또는 긴 본문이 들어간 SRS 알림 상황을 만든다.
2. 워치에서 표시 형태를 확인한다.

기대 결과:

- 워치 알림은 읽기 가능한 짧은 길이로 보인다.
- 과도한 텍스트나 불필요한 상세 내용이 그대로 노출되지 않는다.

---

## 4. 예외 시나리오

### 시나리오 4 - 알림 권한 거부

1. 폰의 `POST_NOTIFICATIONS` 권한을 거부한다.
2. SRS 조건을 만족시킨다.
3. 폰과 워치 모두 알림이 뜨지 않는지 확인한다.

기대 결과:

- 권한이 없는 상태에서는 워치 미러링도 발생하지 않는다.
- 앱은 권한 안내 또는 설정 유도 흐름으로 복구할 수 있어야 한다.

---

### 시나리오 5 - 폰 미연결 상태

1. 워치와 폰 연결을 끊는다.
2. 폰에서 알림을 생성하거나 기존 알림 상태를 확인한다.

기대 결과:

- direct watch push는 발생하지 않는다.
- 미러링 기반이므로 워치 수신은 폰 알림 생성과 폰-워치 연결 상태에 의존한다.
- 같은 Wi-Fi에 있지 않아도 Bluetooth 또는 사용 가능한 네트워크 경로가 있으면 미러링이 가능할 수 있다.

---

## 5. 데모 체크포인트

| Test ID | 확인 항목 | 확인 방법 | 기대 결과 | Pass/Fail | 비고 |
| --- | --- | --- | --- | --- | --- |
| TC-WN-01 | SRS 알림 미러링 | 폰 알림 생성 후 워치 확인 | 동일 logical event가 워치에 표시됨 |  |  |
| TC-WN-02 | due 상태 갱신 | due 상태 변경 후 새 알림 확인 | 최신 알림 상태가 워치에 반영됨 |  |  |
| TC-WN-03 | 내용 길이 검증 | 긴 제목/본문 알림 생성 | 워치에서 짧고 읽기 쉽게 노출됨 |  |  |
| TC-WN-04 | 알림 권한 거부 | 권한 off 상태에서 검증 | 폰/워치 모두 알림 미노출 |  |  |
| TC-WN-05 | 폰 미연결 | 연결 해제 후 확인 | direct push 없이 미러링 정책 유지 |  |  |

---

## 6. 합격 기준

- 폰이 알림 SSOT로 유지된다.
- 워치는 direct push 없이 SRS 알림을 안정적으로 mirrored surface로 수신한다.
- 워치 알림 제목/본문이 짧고 읽기 가능하게 노출된다.
- 과도한 개인정보나 불필요한 상세 데이터가 워치 알림에 노출되지 않는다.
- 미러링 정책이 direct watch push 없이 일관되게 유지된다.
